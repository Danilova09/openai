import { FC } from 'react'
import { GenerateImageComponent } from '../components/generate-image'

export const HomePageComponent: FC = (): JSX.Element => {
	return (
		<>
			<GenerateImageComponent />
		</>
	)
}
